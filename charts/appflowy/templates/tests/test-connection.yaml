apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appflowy.fullname" . }}-test-connection"
  labels:
    {{- include "appflowy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appflowy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
